{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "devCommand": "vite --port 3000 --host",
  "functions": {
    "api/pdf/generate.prod.ts": {
      "memory": 2048,
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/pdf/generate",
      "destination": "/api/pdf/generate.prod"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
